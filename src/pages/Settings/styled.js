import styled from 'styled-components';
import { RankingContainer, RankingTitle } from '../Ranking/styled';

export const SettingsContainer = styled(RankingContainer)``;

export const SettingsTitle = styled(RankingTitle)``;
